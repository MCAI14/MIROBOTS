# MIROBOTS — operational system (reset)

O conteúdo antigo foi movido para `archive_YYYYMMDD_HHMMSS` dentro deste workspace.

Criámos um esqueleto inicial em `new-os/` com um projecto Rust mínimo para começares a desenvolver o novo sistema operativo.

Vê `new-os/README.md` para instruções específicas do esqueleto Rust.
